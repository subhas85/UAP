# helpdesk pipeline (starter, not yet populated)

This is a placeholder for the `helpdesk` ICM pipeline starter. The directory exists so the wizard knows the slot is reserved.

When populated (v1.0.0 target), it will contain numbered stage folders (`01_intake/`, `02_*/`, etc.) each with its own `CONTEXT.md` specifying Inputs / Process / Outputs.

See `../CLAUDE.md` for the planned shape across all workflows.
